import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cv-portfolio-front',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
